# Next.js Pages Router Example

Minimal Pages Router app for checking `react-render-recorder` in a Next.js project.

```sh
pnpm install --ignore-workspace
pnpm dev
```

Open `http://localhost:3000` and use the buttons to create React commits. The recorder script is loaded from `pages/_document.jsx` before the app hydrates.
